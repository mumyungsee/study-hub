# 2026-07-05 — 강의 최종본 복원 + 잔재 파일 정리

## 02:xx — "강의 때 버전이 어디 있지?" 미스터리 추적 → 복원

**고민**: 강의할 때 쓰던 study-hub 사이트랑 지금 사이트가 달라 보임. "이전 버전 같다"는
느낌인데 강의 최종본이 어디 있는지 못 찾겠음. 그걸 기준으로 강의 내용을 정리하고 싶은데.

**조사로 밝힌 것**:
- 지금 이 폴더 = 2026-07-04에 GitHub에서 새로 clone한 것(reflog에 clone 한 줄뿐). 로컬 수정 흔적 0.
- study-hub 레포의 **마지막 커밋 = 6/10**. 그 이후 완전 정지(7/3 다른 레포 대거 정리 때도 study-hub만 안 건드려짐). 이 컴퓨터에 사본은 이거 하나뿐.
- 강의 = **2026-05-20**(영상 기준). 그때 상단 메뉴 4개(AI 메타학습 없음), 환경세팅=Antigravity.
- 강의본 `how-to-use`("지피터스 커뮤니티 야무지게 활용하기") = 5단계 학습 사이클(자발/아웃풋/**병렬**/정제/**자산**) + 부스터. → `bf76f42`(5/19)에 박제.
- **5/23 "학습 사이클 재설계"(`65bb3dd`)에서 강의본이 거꾸로 축소됨**: 병렬 학습→적극 학습, 자산 학습 카드 삭제. 이 축소본이 6/10까지 그대로 유지 = 유저가 본 "이전 버전 같은" 그것.
- 5/25 메타학습 메뉴 신설 + Antigravity→VS Code 전환. (메타학습은 기존 걸 덮은 게 아니라 "추가"였음)

**결정**:
1. **`how-to-use.astro`만 강의본(`bf76f42`)으로 복원**, 나머지 6/10 변경은 전부 유지.
   - 유저 확인: "지피터스 커뮤니티 활용하기(=how-to-use)는 강의본이 맞고, 나머지는 현재(6/10)가 맞다."
   - 커밋 `23de7dc` → push → vercel 배포.
2. **안 쓰는 잔재 파일 2개 삭제** (참조 0 확인 후):
   - `how-to-use-old.astro` — 5/19 이전 옛 온보딩("이 스터디 사용법"). `/onboarding/how-to-use-old/`로 실제 배포되고 있었음(중복 페이지).
   - `01-tools.md` — chapter 99, status 초안인 폐기본(현재 1장은 `00-preview` 담당).
   - 커밋 `d2f6ff9` → push → 배포.

**왜**:
- 강의 영상 스크린샷으로 대조하니 유저 직감("이전 버전 같다")이 정확했음 — 5/23 재설계가 강의 최종본을 되돌린 것.
- 근본 원인: 강의 후 5/23에 사이클을 손봤는데(개악), 그게 왜 바뀌었는지 기록이 없어 "뭐가 최신인지" 혼란. (folder-rules "커밋+push로 박제"의 반대 사례 — git엔 다 남아있어 결국 복원됨)
- `how-to-use`는 `Base.astro`만 import + 자체 `<style>`이라 5/26 스타일 통합과 안 부딪힘 → 5/19 파일을 6/10 환경에 넣어도 빌드 OK(44→43페이지 검증 통과).

**미완/보류**:
- **C등급 튜토리얼 파일명 넘버링**: `05` 두 개(`05-deploy`=5장, `05-fork-clone`=3장), `08` 없음, 파일명 번호 ≠ 실제 chapter. 사이트 표시 순서는 chapter 기준이라 수강생에겐 정상, **파일명만 꼬임**. 고치면 URL 바뀌어 기존 공유 링크·북마크 깨질 위험 → 보류(유저가 강의 때 특정 URL 공유했는지 미확인).
- 커리큘럼 여러 곳 분산 문제(`curriculum-sync-issue.md`, 23기 Phase 5 건).
- `course-renewal`로 살릴 콘텐츠(부록 B·K·C·A) 이주 — 별도 프로젝트.

→ 정본: 강의 최종본 = `bf76f42`(5/19) 시점 = 지금 복원됨. study-hub는 6/10 이후 이번 두 커밋(7/5)이 첫 재개.
