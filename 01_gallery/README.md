# 갤러리

수강생 사례글이 사이트에 공개되는 공간입니다.

## 강사 운영 방법

1. 수강생이 PR 제출 → `00_missions/` 에 파일 도착
2. PR 검토 후 승인
3. 채팅창에 `/gallery` 입력
4. 등록할 사례 선택
5. 자동으로 `src/content/gallery/` 로 이동 + 커밋 + push
6. Vercel 자동 배포 → 사이트 `/gallery/` 페이지에 게시

## 게시된 사례 위치

`src/content/gallery/` — 여기에 있는 파일이 사이트에 공개됩니다.
